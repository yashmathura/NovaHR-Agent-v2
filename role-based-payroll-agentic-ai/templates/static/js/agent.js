const form=document.getElementById('chat-form'), input=document.getElementById('chat-input'), messages=document.getElementById('messages');
function add(text,who){const d=document.createElement('div');d.className='bubble '+who;d.innerHTML='<b>'+ (who==='user'?'You':'NovaHR Agent')+'</b><p>'+text.replace(/\n/g,'<br>')+'</p>';messages.appendChild(d);messages.scrollTop=messages.scrollHeight;return d}
document.querySelectorAll('.suggestions button').forEach(b=>b.onclick=()=>{input.value=b.textContent;form.requestSubmit()});
form.onsubmit=async e=>{e.preventDefault();const text=input.value.trim();if(!text)return;add(text,'user');input.value='';const loading=add('Thinking and checking ERP data…','bot');try{const r=await fetch('/agent/api/',{method:'POST',headers:{'Content-Type':'application/json','X-CSRFToken':document.cookie.split('csrftoken=')[1]?.split(';')[0]||''},body:JSON.stringify({message:text})});const data=await r.json();loading.remove();add(data.message||'No response.','bot')}catch(err){loading.remove();add('Agent request failed. Check the Django server and OpenAI configuration.','bot')}};
